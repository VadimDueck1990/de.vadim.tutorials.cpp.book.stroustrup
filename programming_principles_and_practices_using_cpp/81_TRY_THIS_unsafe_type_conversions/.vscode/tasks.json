{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build word detector",
            "type": "shell",
            "command": "g++",
            "args": ["-g", "-o", "unsafe_conversion", "unsafe_conversion.cpp"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}